
# credentials
{
        service: sDid,
        user:{
            id: uInfo.did,
            keyID: uInfo.keyID,
            sig: uInfo.sig,
            filesMeta: uInfo.filesMeta
        },
        verifier:{
            id: vInfo.did,
            keyID: vInfo.keyID,
            sig: vInfo.sig,
            endpoint: vInfo.endpoint
        }
}